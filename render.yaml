services:
  - type: web
    name: linkchat-backend
    env: python
    rootDir: backend
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    preDeployCommand: python manage.py migrate
    startCommand: daphne -b 0.0.0.0 -p $PORT core.asgi:application
    # Optionally set a health check path if you have one
    # healthCheckPath: /api/health/
    # Configure env vars in the Render dashboard or here if desired
    # envVars:
    #   - key: SECRET_KEY
    #     sync: false
    #   - key: ALLOWED_HOSTS
    #     value: "your-render-service.onrender.com,localhost,127.0.0.1"
    #   - key: CORS_ALLOWED_ORIGINS
    #     value: "https://your-frontend-domain.vercel.app"
    #   - key: REDIS_URL
    #     sync: false
    #   - key: DB_NAME
    #     sync: false
    #   - key: DB_USER
    #     sync: false
    #   - key: DB_PASSWORD
    #     sync: false
    #   - key: DB_HOST
    #     sync: false
    #   - key: DB_PORT
    #     value: "5432"
